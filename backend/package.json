{
  "name": "@moments-forever/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.4"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@moments-forever/shared": "*",
    "@moments-forever/types": "*",
    "@supabase/supabase-js": "^2.55.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.4",
    "tsx": "^4.20.0",
    "typescript": "^6.0.3"
  }
}
